import { colors } from './colors'
import { fonts } from './fonts'
import { buttons } from './buttons'

export const theme = {
  colors,
  fonts,
  buttons,
  borderRadius: {
    sm: '5px',
    md: '21px'
  },
  activityOpacity: 0.8
 }