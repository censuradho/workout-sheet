import { colors } from "./colors";

export const cardWeek = {
  primary: {
    background: colors.primary,
    dayLetter: colors.black,
    dayNumber: colors.darkGray,
  },
  transparent: {
    background: 'transparent',
    dayLetter: colors.white,
    dayNumber: colors.darkGray,
  }
}